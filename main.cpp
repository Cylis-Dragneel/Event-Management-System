#include "database.h"
#include "user.h"
#include "event.h"
#include "venue.h"
#include "registration.h"
#include "budget.h"
#include "validation.h"
// ????????????????????????????????????????????????????????????????
//  main.cpp — Entry point
//  Member 1 (Taha): Add your GUI/menu code inside main()
//  All database setup is handled automatically below
// ????????????????????????????????????????????????????????????????
int main() {
    // ?? Step 1: Open database (always first) ?????????????????????
    if (!openDatabase()) return 1;
    // ?? Step 2: Create tables if they don't exist ????????????????
    createTables();
    // ?? Step 3: Insert seed data if database is empty ????????????
    seedData();
    // ????????????????????????????????????????????????????????????
    //  Taha (Member 1) — add your GUI/menu code here
    // ????????????????????????????????????????????????????????????
    // ?? Last step: Close database (always last) ???????????????????
    closeDatabase();
    return 0;
}