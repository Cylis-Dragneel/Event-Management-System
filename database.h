#pragma once
#include <string>
#include "sqlite3.h"

using namespace std;

// ??? Shared DB handle ????????????????????????????????????????????
extern sqlite3* db;

// ??? Core database functions ?????????????????????????????????????
bool   openDatabase(const string& path = "eventmanager.db");
void   closeDatabase();
void   createTables();
void   seedData();
void   clearAllData();

// ??? Role helpers ????????????????????????????????????????????????
string getUserRole(int user_id);
bool   isOrganizer(int user_id);
bool   isUser(int user_id);